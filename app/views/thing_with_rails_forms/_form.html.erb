<%= form_with(model: thing_with_rails_form, local: true) do |form| %>
  <% if thing_with_rails_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(thing_with_rails_form.errors.count, "error") %> prohibited this thing_with_rails_form from being saved:</h2>

      <ul>
      <% thing_with_rails_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
